# Déploiement 1-clic sur Render (https://render.com) : « New + » → Blueprint → ce repo.
services:
  - type: web
    name: feux-france
    runtime: python
    plan: starter         # toujours actif (7 $/mois) ; "free" possible mais avec mise en veille
    buildCommand: "echo 'aucune dépendance'"
    startCommand: "python3 server.py"
    healthCheckPath: /
